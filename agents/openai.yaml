interface:
  display_name: "FinalsPilot"
  short_description: "Turn messy course materials into a finals sprint"
  default_prompt: "Use $finals-pilot to turn my course materials into a 10-12 hour finals sprint. First check file-reading tools and install/enable missing PDF/PPT/OCR/transcription capabilities when permitted; include guided courseware review and ask before creating scheduled check-ins."

policy:
  allow_implicit_invocation: true
